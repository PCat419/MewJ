"""MewJ 经验参数中心（类似模型权重的统一存放点）。

所有通过调参得到的经验数值集中在此，各模块通过 ``from .params import PARAMS``
读取。结构性常量（花色/风位映射、牌表、路径、阈值公式的结构）仍留在各模块。

调整参数后建议运行：
    python -m unittest discover -s MewJ -t . -p "test_*.py"
并重跑双谱回归确认影响面。
"""

from __future__ import annotations

PARAMS = {
    # ------------------------------------------------------------------
    # 威胁建模（defense.py）
    # ------------------------------------------------------------------
    "defense": {
        # 副露威胁的放铳置信系数（副露数越多越可能已听牌；立直恒为 1.0）
        "furo_alphas": {1: 0.20, 2: 0.40, 3: 0.70, 4: 1.00},
    },
    # ------------------------------------------------------------------
    # 危险度信号系数（defense_heuristics.py，乘算后归一化）
    # >1 抬危险，<1 降危险；总量受 aggregate_min/max 截断
    # ------------------------------------------------------------------
    "danger": {
        # 手切赤五 → 同花色 3/7 危险度下调（赤五早切说明 37 已不需要）
        "red_five_37": 0.70,
        # 前 6 巡手切 2/8 → 早外 1/9 危险度下调
        "early_outside": 0.80,
        # 染手判读：被染花色危险度上调
        "flush_suit": 1.35,
        # 染手判读：字牌危险度小幅上调
        "honitsu_honor": 1.10,
        # 役相关候选（三色/一气等缺失部分）危险度上调
        "yaku_related": 1.20,
        # 带幺系候选数牌危险度上调
        "terminal_yaku": 1.15,
        # 带幺系候选字牌危险度小幅上调
        "chanta_honor": 1.08,
        # 宝牌本身：大幅上调（对手倾向留宝牌做搭子/和牌，放铳损失也大）
        "dora_self": 1.50,
        # 宝牌 ±1 邻牌：小幅上调
        "dora_near1": 1.10,
        # 宝牌 ±2 间牌：小幅上调
        "dora_near2": 1.10,
        # 立直宣言牌跨筋（±1/±2）：小幅上调
        "riichi_decl_near": 1.10,
        # 单牌信号连乘后的总上下限
        "aggregate_min": 0.55,
        "aggregate_max": 1.80,
        # 早外判读的巡数窗口（前 N 巡手切）
        "early_outside_discards": 6,
        # 染手判读所需的最少舍牌数
        "flush_read_discards": 8,
    },
    # ------------------------------------------------------------------
    # 攻防姿态（posture.py）
    # ------------------------------------------------------------------
    "posture": {
        # 手牌期望值极高（约满贯级确定手）→ 威胁下仍全牌效
        "high_ev": 5000.0,
        # 4 位且落后领先者达到该值 → 全牌效（不写死防守）
        "despair_margin": -15000.0,
        # 兜牌对手牌强度的要求（1 向听时）
        "maneuver_min_ev": 2000.0,
        "maneuver_min_uke": 12,
        # 强度稍欠（EV 1500~2000）的 1 向听：仅当存在 ≤cap 的低风险维持牌时才放行兜牌
        "maneuver_min_ev_low_risk": 1500.0,
        "maneuver_low_risk_cap": 0.02,
        # 兜牌可接受的单牌综合危险度上限（多威胁时收紧）
        "maneuver_danger_cap": 0.05,
        "maneuver_danger_cap_multi": 0.03,
        # 触发姿态评估的最小副露数（单副露不触发）
        "min_furo_trigger": 2,
        # ≥ 该副露数视为硬威胁（≈立直，可判全弃）；仅 2 副露为软威胁（地板兜牌）
        "hard_furo_count": 3,
        # 全弃判定：与最小危险度差 ≤ 该值视为等价最优
        "fold_danger_tier": 0.001,
        # 全弃判定：危险度超出最小值但该范围以内视为尚可
        "fold_accept_slack": 0.01,
        # 兜牌拆听门控（0→1 向听）：EV 增益 ≥ 该值才放行（与出牌层同口径）
        "retreat_from_tenpai_ev_min": 800.0,
        # 安全阀：退向候选危险度低于保听候选达到该值时豁免门控
        "retreat_safety_margin": 0.01,
        # 兜→全弃门控：≥1 张完全安牌（危险度=0），或 ≥2 张 ≤该值的低危牌
        "fold_low_risk_tile_cap": 0.01,
        # 全弃口径的综合效用 = −危险度 × 该系数（仅影响显示与 softmax）
        "fold_safety_scale": 10000.0,
        # ---- 形听（KEITEN）：晚巡 1 向听面对硬威胁时，允许打低危牌直达听牌 ----
        # （含无役形听/振听，罚符对待牌质量免疫）保听收流局罚符
        # 形听最早巡目：13 巡起流局罚符期望才开始显著（对应 ryukyoku_base 跳升段）
        "keiten_min_turn": 13,
        # 形听路线候选的综合危险度上限（单威胁/多威胁）；比兜牌宽松：
        # 形听只要求「打到流局」，不是「和出」，可接受略高的放铳风险
        "keiten_danger_cap": 0.10,
        "keiten_danger_cap_multi": 0.12,
        # 形听路线的最低罚符信用（点）：信用 = 流局率 × 罚符期望，巡目越晚越高，
        # 该门槛构成「越晚收益越高」的经济闸，早巡低流局率时不会误入形听
        "keiten_min_credit": 200.0,
    },
    # ------------------------------------------------------------------
    # 荒牌流局罚符期望（noten.py）：nanikiru 的 exp_score 不含不听罚符价值，
    # 本节把该缺口建模为 noten_credit 注入切牌综合效用
    # ------------------------------------------------------------------
    "noten": {
        # 总开关；关闭时 noten_credit 恒为 0（报告列保持稳定）
        "enabled": True,
        # 罚符场上总量（日麻规则恒定 3000 点：1听3未听→未听各付1000/听收3000；
        # 2听2未听→各±1500；3听1未听→未听付3000/听各收1000；全听/全未听无收付）
        "pool": 3000.0,
        # 对手听牌率先验：立直者规则上必听牌
        "p_riichi": 1.0,
        # 四副露 = 单骑必听
        "p_furo4": 1.0,
        # 三副露按听牌处理（≈听牌但未必，打 9 折）
        "p_furo3": 0.9,
        # 其余对手（含 1~2 副露）视为已弃和/未听
        "p_other": 0.0,
        # 已听牌候选把听牌保持到流局的保有系数（每存在 1 个硬威胁乘一次；
        # 硬威胁推进牌局使流局前自家手牌可能被破坏/被迫弃和的概率）
        "keep_base": 0.85,
        # 流局率随「推进者」（立直或 ≥3 副露）数量的衰减系数
        "pusher_decay": 0.85,
        # 流局率巡目基底表 [(最大巡目, 概率)]：越晚越高；
        # 13 巡起进入显著区，终巡附近约 0.5（经验标定，未做样本校准）
        "ryukyoku_base": [
            (8, 0.02),
            (9, 0.04),
            (10, 0.05),
            (11, 0.07),
            (12, 0.09),
            (13, 0.12),
            (14, 0.18),
            (15, 0.25),
            (16, 0.32),
            (17, 0.40),
            (99, 0.50),
        ],
    },
    # ------------------------------------------------------------------
    # 评分与权重（scoring.py）
    # ------------------------------------------------------------------
    "scoring": {
        # softmax 温度（EV 尺度；越大权重越平）
        "temperature": 200.0,
        # 线外候选权重下限（显示 <0.01%）
        "min_recommendation_weight": 1e-6,
        # 等价最优的综合效用差 ε
        "equivalent_utility_epsilon": 1.0,
        # 实切权重 ≥ 最优 × 该比例 → 判定「尚可」
        "acceptable_weight_ratio": 0.85,
        # 放铳损失估计（未标定，透明常数；荣和庄家/闲家）
        "riichi_loss_child": 6000.0,
        "riichi_loss_dealer": 9000.0,
        "furo_loss_child": 3900.0,
        "furo_loss_dealer": 5800.0,
        # 每场叠加的本场奖励
        "honba_bonus": 300.0,
        # 进攻欲隐藏旋钮：1.0 中性，低偏守高偏攻
        "min_offensive_desire": 0.45,
        "max_offensive_desire": 1.75,
        "default_offensive_desire": 1.0,
        # 分差项饱和参照（约一把大牌的点差）
        "offense_margin_ref": 12000.0,
        "max_rank_term": 0.30,
        "max_margin_term": 0.35,
        # nanikiru 无统计时的牌效效用公式系数：
        # (4−向听)×shanten_coef + log1p(进张)×uke_coef + 听牌率×tenpai_coef + 和率×win_coef
        "util_shanten_coef": 900.0,
        "util_uke_coef": 180.0,
        "util_tenpai_coef": 800.0,
        "util_win_coef": 1200.0,
        # 同向听进攻效用：EV + 和率×win_scale + 听牌率×tenpai_scale。
        # 交换率含义：1pp 和率 ≈ win_scale/100 点 EV；高和率能否翻盘取决于 ΔEV 与 Δ和率的相对大小，无绝对 EV 门槛。
        "near_tie_win_scale": 1000.0,
        "near_tie_tenpai_scale": 200.0,
        # 高向听烂牌的役牌单张保有补正：δ=(向听−2)×base×活牌比例（≥该向听才补）
        "yakuhai_keep_base": 60.0,
        "yakuhai_keep_min_shanten": 4,
    },
    # ------------------------------------------------------------------
    # 副露判断（call_eval.py）：碰/吃 vs 跳过的反事实评估
    # ------------------------------------------------------------------
    "call": {
        # 速度轴：副露后和率 − 跳过期望和率 ≥ 该值 → 推荐副露
        "win_prob_delta_min": 0.10,
        # 速度轴比例旁路：和率比（WP_call/WP_skip）≥ 该值 且 Δ和率 ≥ win_prob_delta_floor
        # → 与绝对差路径同等视为速度轴合格。覆盖和率绝对差略低于
        # win_prob_delta_min 但比例显著占优的情形
        "win_prob_ratio_min": 1.5,
        "win_prob_delta_floor": 0.03,
        # 收益轴：ΔEV > 该值 → 推荐副露。「已有役牌低值手继续提速」的
        # 边际正 EV 不应单独驱动副露，与 review 层退向门控 800 同口径
        "ev_margin": 800.0,
        # 自杀层：副露后和率归 0 → 否决（防无役碰/强行退向）。
        # EV 比值否决（副露 EV < 该比例 × 跳过 EV）仅在 >0 时启用，默认 0.0 禁用：
        # EV 退化严重的变体由双轴自然过滤（Δ和率<0.10 且 ΔEV≤800 不会合格）
        "suicide_ev_ratio": 0.0,
        # 报告权重的显示校准（仅用于显示，不参与判定）：
        # 速度轴合格变体的显示效用差 d = Δ和率 × 该系数（相对跳过基准 0）。
        # 标定依据：T=200 下 Δ和率 0.10~0.23 → 权重 98%~99.99%，与 mortal 置信度同量级
        "win_prob_weight_scale": 8000.0,
        # v1 不评估大明杠（岭上摸牌期望需另建模型）
        "eval_daiminkan": False,
        # ---- 形听轴：晚巡 1 向听时允许无役副露（吃/碰）+ 低危切保听避罚符 ----
        # 语境（姿态/巡目/威胁/危险查询）由 review 调用处传入；全弃态轴关闭
        # 启用形听轴的最早巡目（与 posture.keiten_min_turn 同口径，13 巡起）
        "form_tenpai_turn_min": 13,
        # ΔP听 门槛：副露后必听 vs 跳过侧终局前听牌率（摸牌聚合口径）之差
        "form_tenpai_delta_min": 0.3,
        # 形听价值闸（点）：ΔP听 × 流局率 × (E听 − E未听) ≥ 该值才推荐。
        # 标定：单立直摆幅 2500 点，15 巡流局率≈0.21 → ΔP听=0.35 时约 185 点；
        # 取 150 使中晚巡有效、早巡被流局率自动压死（越晚收益越高）
        "form_tenpai_value_min": 150.0,
        # 副露后保听切的综合危险度上限（单威胁/多威胁；
        # 与 posture.keiten_danger_cap 同口径 0.10/0.12）
        "form_tenpai_danger_cap": 0.10,
        "form_tenpai_danger_cap_multi": 0.12,
    },
    # ------------------------------------------------------------------
    # 出牌层退向阈值（review.py）
    # ------------------------------------------------------------------
    "review": {
        "retreat": {
            # 拆听（0→1）：和率不降则放行；和率下降时用
            # ΔEV + Δ和率×win_scale + Δ听牌率×tenpai_scale ≥ 0 动态过闸。
            # 无和率统计时回退到 from_tenpai_ev_min。
            # win_scale 高于同向听破局（和率在听牌态更贵：先制/立直/罚符）。
            "from_tenpai_ev_min": 800.0,
            "from_tenpai_win_scale": 8000.0,
            "from_tenpai_tenpai_scale": 500.0,
            # 普通退向 EV 门槛：min(cap, base + per_turn×(巡−5)) × (1 + gap_coef×(深度−1))
            "delta_ev_base": 50.0,
            "delta_ev_per_turn": 20.0,
            "delta_ev_cap": 350.0,
            "delta_ev_gap_coef": 0.5,
            # 普通退向听牌率门槛：min(cap, base + per_turn×(巡−5)) + per_gap×(深度−1)，封顶 hard_cap
            "tenpai_rate_base": 0.50,
            "tenpai_rate_per_turn": 0.03,
            "tenpai_rate_cap": 0.90,
            "tenpai_rate_per_gap": 0.05,
            "tenpai_rate_hard_cap": 0.95,
        },
    },
}
